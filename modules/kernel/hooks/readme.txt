SpawnEngine Kernel Hooks
These hooks manage real-time Mesh updates, wallet state, alerts, creator data, and theme engine.
Each hook is modular and can be imported directly into components or GlobalState.